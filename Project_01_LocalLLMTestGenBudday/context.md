# Context

- **Current Action**: All 4 phases complete. Fixed model-not-found bug by adding dynamic model selection. Application verified and running (backend port 3001, frontend port 5173).
- **Previous Action**: Added `/api/models` endpoint and Ollama Model dropdown to Settings UI.
